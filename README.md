# Parker Johnson — Portfolio

Static, GitHub Pages-ready portfolio site.

## Before publishing

Update the LinkedIn URL and email address in `index.html` with your real details. The site has no build step: GitHub Pages can publish the repository root directly.

## Local preview

Open `index.html` directly, or run any local static server from this folder.
